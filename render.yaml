services:
  - type: web
    name: petrol-pump
    runtime: python
    buildCommand: |
      apt-get update
      apt-get install -y tesseract-ocr
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn petrol_pump.wsgi:application